tasks:
- install:
- ceph:
- rgw: [client.0]
- s3tests:
    client.0:
      git_remote: https://github.com/cbodley/
      force-branch: wip-pr-266-ceph-master
      rgw_server: client.0
overrides:
  ceph:
    conf:
      client:
        rgw lc debug interval: 10
